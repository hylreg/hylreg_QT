[project]
name = "hylreg-demo-workspace"
version = "0.1.0"
description = "Demo 工作区：包含多个子项目"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["demo/hylreg_hub_manager"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
